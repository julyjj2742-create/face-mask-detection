# Pretrained Models

This project uses the YOLOv8n pretrained weights.

These weights are **not stored** in this repository.  
They are downloaded automatically when running:

```python
from ultralytics import YOLO
model = YOLO("yolov8n.pt")
