# Processed Data

For this project, preprocessing includes:
- Converting Pascal VOC XML annotations to YOLO format
- Creating `train/` and `val/` split directories
- Generating the `face_mask.yaml` configuration file

All preprocessing steps are documented and executed inside the notebook.
